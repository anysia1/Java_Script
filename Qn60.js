function rectanglePerimeter(width, height) {
    return 2 * (width + height);
  }
  const rectangleWidth = 5;
  const rectangleHeight = 10;
  const perimeter = rectanglePerimeter(rectangleWidth, rectangleHeight);
  
  console.log(perimeter); 